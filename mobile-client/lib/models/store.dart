class Store {
  final int uid;
  final String name;

  Store(this.uid, this.name);

  // User.fromJson(Map<String, dynamic> json) : uid = json['uid'];

  // Map<String, dynamic> toJson() => {'uid': uid};
}
